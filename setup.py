from setuptools import setup, find_packages

setup(
    name='oblong-exercise',
    packages=find_packages(exclude=['test']),
    version='1.0',
    description='The next generation of note-taking!',
    author_email='richard.b.winslow@gmail.com'
)
